<h2>Reinitiser votre mot de passe?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :"email utilisé lors de l'inscription" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"input-field rounded-md" %>
  </div>

  <div class="actions">
    <%= f.submit "Vous Envoyez les instructions de reinitialisation" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
